# Tests

This directory will host unit and integration tests for the reorganized
LLAMEA baseline project. Follow these steps to run tests once they are
added:

```bash
pip install -e .
pytest
```

Place new test modules under `tests/` and prefer descriptive filenames
such as `test_llm.py` or `test_benchmarks.py`.





